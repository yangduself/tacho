//main, implementation of tach algrothim

#include "info.h"
int fsamp = 1000; //sample rate
//char buf[2000];


int main (void)
{
	readdata(file);
	
	printf("content is: %s\n", buf);
	printf("the first content is: %c\n", buf[0]);
	
	
	return 0;
}